# 줄바꿈 규칙.
#
# 윈도우에서 만든 파일은 줄 끝에 CR 이 붙는다(CRLF). 리눅스 서버가 그대로 실행하는
# 파일에 CR 이 섞이면 bash 는 `$'\r': command not found` 로 죽고, 설정 파일은
# 값 끝에 보이지 않는 글자가 붙는다. 아래 파일들은 저장소에 LF 로만 담는다.
*.sh      text eol=lf
*.mjs     text eol=lf
*.conf    text eol=lf
*.service text eol=lf
