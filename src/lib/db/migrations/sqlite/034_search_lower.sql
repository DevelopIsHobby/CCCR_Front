-- 검색을 대소문자 가리지 않게 바꾼 것에 맞추는 자리.
--
-- 조회문을 `LOWER(칸) LIKE ?` 로 바꿨다(lib/like.ts). SQLite 의 LIKE 는 원래
-- 영문 대소문자를 가리지 않아 동작은 그대로지만, PostgreSQL 과 같은 문장을 쓰려고 맞췄다.
-- SQLite 에는 LIKE '%말%' 에 쓸 수 있는 색인이 없어 여기서 만들 것이 없다.
-- postgres/034 쪽에서만 색인을 다시 만든다. 파일 이름을 짝지어 두려고 이 파일을 둔다.
SELECT 1;
