/*
  보관 기간.

  개인정보 처리방침 제3조에 적은 기간과 여기 숫자가 같아야 한다.
  한쪽만 고치면 적어 놓고 지키지 않는 셈이 되어 그 자체가 문제가 된다.
  기간을 바꿀 때는 legal-data.ts 의 제3조(와 이용약관 제6조·제12조)도 함께 고친다.

  여기 없는 기간
    - 서버 접속 기록 90일 → Nginx 가 남기므로 deploy/logrotate-nginx 에서 정한다
    - 휴지통 30일        → src/lib/db/trash.ts
    - 백업 30일          → scripts/backup.sh
*/
export const RETENTION_DAYS = {
  /** 교육사업 제안 — 처리 완료일부터 */
  proposal: 365,
  /** 반려된 사업공고 수신신청 — 반려일부터 */
  noticeRejected: 180,
  /** 해지한 뉴스레터·수신 중단한 사업공고 — 해지·중단일부터. 그 사이 실수로 끈 것은 되돌릴 수 있다 */
  unsubscribed: 30,
  /** 접속 통계 기록 */
  visits: 90,
  /** 안내 메일 발송 기록 — 받는 이메일주소가 담기므로 방침 제2조·제3조에 적었다 */
  mailLog: 365,
  /** 횟수 제한 기록 — 세는 창이 한 시간이라 하루면 넉넉하다 */
  rateEvents: 1,
  /** 가입 승인을 기다리는 신청 — 신청일(또는 대기로 되돌린 날)부터 */
  pendingMember: 180,
  /** 이용이 제한된 계정 — 제한한 날부터 */
  blockedMember: 365,
  /**
    관리자 접속 기록 — 기록일부터.
    「개인정보의 안전성 확보조치 기준」 제8조는 1년 이상을 요구한다.
    점검하다 놓친 것을 되짚을 수 있게 넉넉히 2년 둔다.
  */
  adminAccess: 730,
} as const;
