/*
  보관 기간.

  개인정보처리방침 제4조에 적은 기간과 여기 숫자가 같아야 한다.
  한쪽만 고치면 적어 놓고 지키지 않는 셈이 되어 그 자체가 문제가 된다.
  기간을 바꿀 때는 legal-data.ts 의 제4조도 함께 고친다.
*/
export const RETENTION_DAYS = {
  /** 교육사업 제안 — 처리 완료일부터 */
  proposal: 365,
  /** 반려된 사업공고 수신신청 — 반려일부터 */
  noticeRejected: 180,
  /** 해지한 뉴스레터·수신 중단한 사업공고 — 해지·중단일부터. 그 사이 실수로 끈 것은 되돌릴 수 있다 */
  unsubscribed: 30,
  /** 접속 기록 */
  visits: 90,
  /** 메일 보낸 기록 — 방침에 적는 개인정보는 아니지만 무한히 쌓이면 곤란하다 */
  mailLog: 365,
  /** 횟수 제한 기록 — 세는 창이 한 시간이라 하루면 넉넉하다 */
  rateEvents: 1,
} as const;
